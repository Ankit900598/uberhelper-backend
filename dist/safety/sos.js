/**
 * SOS scaffolding for MVP.
 * In production:
 * - store row in `sos_events`
 * - notify admin control room
 * - optionally call police/helpline (integration)
 */
export async function raiseSos(deps, input) {
    // Pseudocode:
    // await deps.db.insertSosEvent(input)
}
